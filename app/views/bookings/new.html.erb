<div class="container", id="show-animal">
  <div class="container-info">
  </div>
  <div class="container-options">
    <h1>Book <%= @animal.name %>, the <%= @animal.species %></h1>
    <p>When do you want to book our dear <strong><%= @animal.name %></strong>?</p>
    <%= render "form", animal: @animal, booking: @booking %>

</div>

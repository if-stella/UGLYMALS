<%= simple_form_for(animal) do |f| %>
  <!-- [...] -->
  <!-- <%= f.input :photo, as: :file %> -->
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :species %>
  <%= f.text_field :species %>
  <%= f.label :age %>
  <%= f.number_field :age %>
  <%= f.label :price %>
  <%= f.number_field :price %>
  <%= f.submit %>
  <!-- [...] -->
<% end %>

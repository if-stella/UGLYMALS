<div class="container">
  <div class="container-info">
    <%= render "show", animal: @animal %>
  </div>

  <div class="container-options">
    <%= link_to 'Back to index', animals_path, class: "btn btn-primary" %>
    <%= link_to 'Book', new_animal_booking_path(@animal), class: "btn btn-primary" %>
  </div>

</div>
